# Core dependencies
pydantic==2.5.0
python-dotenv==1.0.0
fastapi==0.110.0
uvicorn==0.29.0
python-multipart==0.0.9

# LLM integration
ollama==0.1.6
chromadb>=0.5.0
onnxruntime>=1.15.0        # Semantic embeddings for ChromaDB (all-MiniLM-L6-v2)

# Video/Audio processing
ffmpeg-python==0.2.0

# Whisper for transcription
openai-whisper
faster-whisper

# WhisperX for word-level alignment
git+https://github.com/m-bain/whisperx.git

# Audio processing dependencies for WhisperX
torch>=2.2.0
torchaudio>=2.2.0

# Additional utilities
colorama==0.4.6
tqdm==4.66.1

# Fine-tuning (Whisper LoRA)
transformers>=4.40.0
datasets>=2.18.0
accelerate>=0.27.0
peft>=0.10.0
evaluate>=0.4.2
jiwer>=3.0.3
soundfile>=0.12.1
